{
  "name": "vibe-match",
  "version": "1.0.0",
  "description": "Find the best MCPs, skills, and agents for your project idea",
  "main": "server.js",
  "scripts": {
    "start": "node --env-file=.env server.js",
    "dev": "node --env-file=.env server.js"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "express": "^4.21.0"
  },
  "devDependencies": {
    "playwright": "^1.61.1"
  }
}
